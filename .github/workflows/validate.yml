name: Validate Learning App

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: |
          node --check app.js
          node --check scheduler.js
          node --check interaction-engine.js
          node --check persistence.js
          node --check backend-bridge.js
          node --check speech-service.js
          node --check sakhi-shell.js
          node --check adaptive-engine.js
          node --check curriculum.js
          node --check kid-upgrade.js
      - name: Check required interaction schema fields
        run: node scripts/validate-activities.cjs
